services:
  - type: web
    name: TrhoveSvinyWeb
    env: python
    buildCommand: python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
    startCommand: ./.venv/bin/gunicorn run:app --bind 0.0.0.0:$PORT
